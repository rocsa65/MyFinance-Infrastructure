version: '3.8'

services:
  # Green Environment - Deployment Target
  myfinance-api-green:
    image: ${DOCKER_REGISTRY}/myfinance-server:${GREEN_RELEASE_NUMBER:-latest}
    container_name: myfinance-api-green
    restart: unless-stopped
    ports:
      - "5002:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Data Source=/data/finance_green.db
      - ASPNETCORE_URLS=http://+:80
      - ENVIRONMENT_COLOR=green
      - RELEASE_NUMBER=${GREEN_RELEASE_NUMBER}
    volumes:
      - green_api_data:/data
      - green_api_logs:/app/logs
    networks:
      - myfinance-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  myfinance-client-green:
    image: ${DOCKER_REGISTRY}/myfinance-client:${GREEN_RELEASE_NUMBER:-latest}
    container_name: myfinance-client-green
    restart: unless-stopped
    ports:
      - "3002:80"
    environment:
      - ENVIRONMENT_COLOR=green
      - RELEASE_NUMBER=${GREEN_RELEASE_NUMBER}
      - REACT_APP_API_URL=http://myfinance-api-green
    networks:
      - myfinance-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    depends_on:
      - myfinance-api-green

volumes:
  green_api_data:
    driver: local
  green_api_logs:
    driver: local

networks:
  myfinance-network:
    external: true